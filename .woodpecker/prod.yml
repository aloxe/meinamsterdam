when:
  - event: push
    branch: [main, try-to-deploy]

steps:
  - name: ⛓️ Use Node et 🛠️ Install 
    image: node
    commands:
     - npm install
     - npm run build

  - name: check a boo
    image: debian
    commands:
      - echo "Testing ..."
      - ls -al

  - name: scp files (╯ ͡° ͜ʖ ͡°）╯／/┴┬┴┬┴ 
    image: appleboy/drone-scp
    settings:
      host: 
        from_secret: HOST
      user: 
        from_secret: USERNAME
      key:
        from_secret: KEY
      target: ~/domains/test.xn--4lj4bfp6d.eu.org/public_html/
      source: _site/*
      strip_components: 1
      rm: true
